apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ui
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    namespace: 'ui'
    server: https://kubernetes.default.svc
  source:
    path: src/ui/chart
    repoURL: https://github.com/Andarol/Kubernetes.git
    targetRevision: main
    helm:
      valueFiles:
        - /retail-store/values/ui.yaml
  sources: []
  project: default
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
