apiVersion: v1
kind: Service
metadata:
  name: demo-app-db
  labels:
    app: demo-app
    component: db
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: demo-app
    component: db
  ports:
    - port: 5432
      targetPort: 5432
