apiVersion: v1
kind: Service
metadata:
  name: demo-app-frontend
  labels:
    app: demo-app
    component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    app: demo-app
    component: frontend
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
