apiVersion: v1
kind: Service
metadata:
  name: simple-app-frontend
  labels:
    app: simple-app
    component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    app: simple-app
    component: frontend
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
