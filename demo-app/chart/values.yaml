global:
  registry: ""

frontend:
  image:
    repository: frontend
    tag: "latest"  # frontend
  replicas: 1
  service:
    type: ClusterIP
    port: 80

backend:
  image:
    repository: backend
    tag: "latest"  # backend
  replicas: 1
  hpa:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 60
  env:
    APP_VERSION: "1.0.0"
  service:
    port: 8080

db:
  image:
    repository: postgres
    tag: "16-alpine"
  storage: 1Gi
  env:
    POSTGRES_DB: appdb
    POSTGRES_USER: appuser
    POSTGRES_PASSWORD: apppass

ingress:
  enabled: true
  host: demo-app.demo-app

tolerations:
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
    effect: NoSchedule
