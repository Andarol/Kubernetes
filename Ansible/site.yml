---
- name: Configure Kubernetes Control Plane
  hosts: control_plane
  become: true
  vars_files:
    - inventories/vault.yml
  roles:
    - k8s_cp

- name: Configure Kubernetes Worker Nodes
  hosts: workers
  become: true
  vars_files:
    - inventories/vault.yml
  roles:
    - k8s_worker
